1. Para ejecutar el proyecto hay que localizarse en la carpeta "api" y ejecutar el comando "npm install" o "npm i" para descargar las librerías necesarias
2. Antes de ejecutar el proyecto hay que crear una base de datos llamada "project", y cambiar los datos necesarios en los archivos "api\.env" y "api\src\db.ts"
3. Una vez descardasa, ejecutamos el comando "npm run dev" para correr el proyecto en modo developer
4. Una vez hecho esto ya podemos ejecutar peticiones a la API, para ello podemos importar el json "api\Postman Peticiones\Project.postman_collection.json" en Postman
